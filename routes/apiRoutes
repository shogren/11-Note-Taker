const router = require("express").Router();

// responds with all notes from db.json
router.get("/notes", function(req, res) {
 
    let data = res.json('./db/db.json')   
    
    return JSON.parse(data);

    }
);

module.exports = router;